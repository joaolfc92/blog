{{#each erros}}

	<div class="alert alert-danger">{{texto}}</div>

{{else}}	


{{/each}}


<h3>Editar  Categoria: </h3>

<div class="card">
  <div class="card-body">

		<form action="/admin/categorias/edit" method="POST">
		
		<div class="form-group">
		<input type="hidden" name="id" value="{{categoria._id}}">
			<label for="exampleInputname">Nome:</label>
			<input type="text" name="nome" class="form-control" id="nome" aria-describedby="emailHelp" placeholder="Enter name" value='{{categoria.nome}}'>
		
		</div>
		<div class="form-group">
			<label for="exampleInputCategorias">Slug</label>
			<input type="text" name="slug" class="form-control" id="slug" placeholder="Slug da categoria" value='{{categoria.slug}}'>
		</div>
		
		<button type="submit" class="btn btn-success">Editar Categoria</button>
		</form>
	</div>
</div>		

